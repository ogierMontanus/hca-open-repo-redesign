"""Shared primitives for the cleaning pipeline.

Only things genuinely used by more than one caller live here. A helper with
one consumer belongs next to that consumer, where its assumptions are visible.
"""
